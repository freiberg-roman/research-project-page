import React from "react";
import { styled } from "@pigment-css/react";
import { SIZES } from "../../util/const";

function VideoBlock({title, children}) {
    return (
        <>
        Video
        </>
    )
}

export default VideoBlock;
